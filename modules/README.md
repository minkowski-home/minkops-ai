# Modules

Reusable business capabilities live here. A module implements a capability such
as document processing, reconciliation, reporting, or scheduling and should be
usable by more than one solution. Customer-specific sequencing and policy stays
in `solutions/`.
