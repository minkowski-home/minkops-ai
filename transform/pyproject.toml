[project]
name = "transform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apache-airflow>=3.1.8",
    "dbt-core>=1.11.7",
    "dbt-postgres>=1.10.0",
    "jupyter>=1.1.1",
]
