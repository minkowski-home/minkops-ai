The system's liveness signal — Ember means running, grey means not.

```jsx
<StatusDot status="active" pulse label="active" />
<StatusDot status="disabled" />
```

Ember appears here more than anywhere else in the product; that is deliberate. Never colour a dot green for "active" — green is reserved for resolved/ok outcomes.
