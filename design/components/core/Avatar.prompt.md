Initial-based identity chip for agents and operators — Minkops has no portrait imagery.

```jsx
<Avatar name="Imel" kind="agent" />
<Avatar name="Kartik Shah" kind="human" size={26} />
```

Agents read as dark graphite tiles, humans as light bordered tiles: the distinction is load-bearing in the task thread.
