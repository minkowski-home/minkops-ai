export interface EyebrowProps {
  /** Extends a hairline rule to the right of the label. */
  rule?: boolean;
  children?: React.ReactNode;
  style?: React.CSSProperties;
}

export declare function Eyebrow(props: EyebrowProps): JSX.Element;
