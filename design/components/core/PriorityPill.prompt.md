Severity summary pill — used in the attention queue header and on interrupt rows.

```jsx
<PriorityPill level="critical" count={2} />
<PriorityPill level="medium" />
```

Always a white pill with a coloured dot; the fill never carries the severity colour.
