A square, label-less control for toolbar and header glyphs.

```jsx
<IconButton label="Notifications"><Icon name="bell" /></IconButton>
<IconButton label="Theme" active><Icon name="palette" /></IconButton>
```

Always pass `label`. Wrap an `<Icon>`; do not put text inside.
