A small classification chip — interrupt type, record state, category.

```jsx
<Badge tone="approval">Approval</Badge>
<Badge tone="escalation">Escalation</Badge>
<Badge tone="neutral" mono={false}>Enterprise tier</Badge>
```

Mono uppercase by default because badges label machine-generated categories. For counts and severity use `PriorityPill` instead.
