The repeating unit of the console's left pane — one hired agent.

```jsx
<AgentRow name="Imel" role="Email handler" status="active" lastActive="4m ago" enabled onToggle={setEnabled} />
```

Selected rows get an Ember inset edge, not a filled background. Disabled agents stay in the list at 55% opacity — they are provisioned, just off.
