Single-line text control.

```jsx
<Input placeholder="you@company.com" type="email" />
<Input mono placeholder="tenant_001" size="sm" />
```

Focus is an Ember 1px border plus a soft Ember ring — the only place Ember touches a form. Pair with `Field`.
