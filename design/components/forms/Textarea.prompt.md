Multi-line text control; `autoGrow` is the task-composer behaviour.

```jsx
<Textarea autoGrow rows={1} placeholder="Describe a task for your agents…" />
```

In the composer, Enter submits and Shift+Enter inserts a newline — state that hint in mono micro-type under the field.
