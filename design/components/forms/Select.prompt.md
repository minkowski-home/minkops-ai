Native select with the system's border, focus ring, and a chevron affordance.

```jsx
<Select options={[{ value: "support", label: "Customer support" }, { value: "sales", label: "Sales & lead gen" }]} />
```

Use for 4+ mutually exclusive choices in a form. For a questionnaire step use `OptionRow` instead — bigger targets, visible options.
