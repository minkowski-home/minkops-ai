Shown in the task thread while an agent is composing.

```jsx
<TypingIndicator name="Synapse" />
```

Says "is working", not "is typing" — agents do work, not chat. Three Ember dots at 1.2s, opacity only.
