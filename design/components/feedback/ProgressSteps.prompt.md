Segmented 3px progress bar for short, known-length flows (the funnel, onboarding).

```jsx
<ProgressSteps total={4} current={1} />
```

Segments, not a continuous bar — the user should be able to count what's left. Pair with an `Eyebrow` reading "Step 02 / Time audit".
