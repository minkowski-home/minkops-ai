Zero-data state for panes and lists.

```jsx
<EmptyState icon={<Icon name="agents" size={20} />} headline="All clear"
  hint="No items need your attention right now. Agents are running smoothly." />
```

Copy stays matter-of-fact and reassuring; no exclamation marks, no illustrations.
