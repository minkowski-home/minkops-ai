"""Capability implementations owned by the AI Suite runtime.

Capabilities are grouped by domain (postgres persistence, email sending, etc.)
so they can be reused across many agents without duplicating code per agent.
"""

