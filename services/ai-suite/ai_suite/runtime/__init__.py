"""Runtime orchestration layer.

This package owns the mechanics of executing agent graphs and coordinating
tools/side effects. It should not contain agent-specific decision logic; that
belongs in the `agents/` package.
"""

