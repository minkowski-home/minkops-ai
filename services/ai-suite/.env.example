# LLM API Keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Ollama Variables
REPLY_LLM_OLLAMA=
REPLY_TEMPERATURE=
CLASSIFICATION_LLM_OLLAMA=
CLASSIFICATION_TEMPERATURE=

# Database Connection
# The runtime reads `AGENTS_DB_URL` (preferred) and falls back to `DATABASE_URL`.
# We keep `DATABASE_URL` here because it's the most common convention.
DATABASE_URL=
PSQL_PATH=
LOG_LEVEL=