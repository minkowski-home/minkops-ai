# LLM API Keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Ollama Variables
REPLY_LLM_OLLAMA=
REPLY_TEMPERATURE=
CLASSIFICATION_LLM_OLLAMA=
CLASSIFICATION_TEMPERATURE=

# Database Connection
# ADMIN_DB_URL        — superuser connection used only by `seed-db` for DDL operations
#                       (DROP/CREATE tables). Typically the OS user or a dedicated admin role.
#                       Falls back to DATABASE_URL if unset (single-URL local dev setups).
# DATABASE_URL        — least-privilege app user connection used by the runtime for all DML.
#                       This user should only have SELECT, INSERT, UPDATE, DELETE.
# MINKOPS_DB_PASSWORD — password for the minkops app role. Used when running db/bootstrap.sql:
#                       psql postgres -v minkops_password=$MINKOPS_DB_PASSWORD -f db/bootstrap.sql
#                       Must match the password in DATABASE_URL.
ADMIN_DB_URL=
DATABASE_URL=
MINKOPS_DB_PASSWORD=
PSQL_PATH=
LOG_LEVEL=