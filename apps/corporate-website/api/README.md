# Corporate Website Backend

Python + FastAPI backend for the corporate website.

## Setup

1. Install dependencies:
   ```bash
   uv sync
   ```
2. Run the server:
   ```bash
   uv run src/main.py
   ```
